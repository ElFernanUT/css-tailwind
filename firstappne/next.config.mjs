// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['*.googleusercontent.com', 'encrypted-tbn0.gstatic.com'],
    },
  };
  
  export default nextConfig;
  